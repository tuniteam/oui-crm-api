// ============================================
// OUI-CRM - Global search constants (US-01-12)
// ============================================

/** 10 results per type (SPEC-07): a launcher, not a list — the lists have their own screens. */
export const SEARCH_LIMIT = 10;

/** Below two characters a search would return half the base. */
export const SEARCH_MIN_LENGTH = 2;
